import './bootstrap';

// Alpine.js is bundled and auto-initialized by Livewire 4.
// Do NOT import Alpine separately to avoid double-instance conflicts.
// If standalone Alpine is needed without Livewire, uncomment:
// import Alpine from 'alpinejs';
// window.Alpine = Alpine;
// Alpine.start();
